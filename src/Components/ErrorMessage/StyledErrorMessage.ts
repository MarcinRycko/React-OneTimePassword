import styled from 'styled-components';

const StyledErrorMessage = styled.div`
  height: 30px;
  margin: 20px 0;
`;

const StyledErrorMessageText = styled.span`
  color: #ff0000;
`;

export { StyledErrorMessage, StyledErrorMessageText };
